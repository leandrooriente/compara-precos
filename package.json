{
  "name": "compara-precos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Calculadora de financiamento ou carro por assinatura que investe a diferença.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
